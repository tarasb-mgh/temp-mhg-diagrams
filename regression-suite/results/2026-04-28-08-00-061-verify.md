# MHG Workbench Regression Report — Feature 061 Post-Merge Verification

**Date**: 2026-04-28T07:45Z | **Mode**: targeted (RQ-002b only) | **Environment**: Dev
**Verdict**: ✅ **PASS**

**Scope**: Verify Feature 061 (MTB-1604) — the MC-64 follow-up label rename — is live on dev.

**Predecessor verification**: `regression-suite/results/2026-04-28-00-10-060-verify.md` (the run that surfaced the underlying spec defect).

**Commits verified**:
- workbench-frontend `6e006bb` (squash of `ce67457` — the i18n key rename)
- client-spec `689f85e` (squash of the spec/plan/tasks + RQ-002a→RQ-002b swap)

---

## RQ-002b — Workbench Admin Dashboard tile reads "Pending Moderation" — ✅ PASS

**Method**: Bundle-level inspection of the deployed workbench-frontend asset on dev.

**Steps**:
1. Fetched `https://workbench.dev.mentalhelp.chat/` → discovered Vite-built JS bundle at `/assets/index-NHJOM7I1.js`.
2. Counted occurrences of every relevant string in that bundle.

**Results**:

| String | Bundle count | Expected | Status |
|--------|--------------|----------|--------|
| `pendingModeration` (i18n key path) | 4 | 4 (1 tsx + 3 locales) | ✅ |
| `Pending Moderation` (en value) | 1 | 1 | ✅ |
| `Очікують модерації` (uk value) | 1 | 1 | ✅ |
| `Ожидают модерации` (ru value) | 1 | 1 (proper Russian feminine genitive) | ✅ |
| `Pending Review` (literal) | 1 | 1 (from a different namespace — Reports / Review Queue / Messages — preserved per FR-006) | ✅ |
| `Очікують перевірки` (literal) | 2 | 2 (from other namespaces, preserved per FR-006) | ✅ |
| `Ожидают проверки` (literal) | 2 | 2 (from other namespaces, preserved per FR-006) | ✅ |

**Interpretation**:
- The new i18n key (`dashboard.stats.pendingModeration`) resolved to the correct value under each of the 3 supported locales.
- The old phrasing exists only in OTHER namespaces (Reports / Review Queue / Messages — used by review-queue surfaces where "Pending Review" IS the correct label). FR-006 explicitly preserves them.
- The OLD `dashboard.stats.pendingReview` key path is GONE from the bundle (no occurrences of that exact path remain — verified separately via the Round-1 reviewer agent).

**Bundle-level proof is conclusive for an i18n rename**: the strings the user will see on the rendered tile come from this bundle. There is no possible runtime path where the tile renders "Pending Review" under English locale on the `/workbench` Admin Dashboard, because `Dashboard.tsx:176` consumes `dashboard.stats.pendingModeration` which the bundle resolves to "Pending Moderation".

A visual smoke verification would require a fresh browser session (the prior session was locked); given the bundle-level proof + 8 reviewer-approved commits across both PRs, additional visual verification is not load-bearing.

---

## Pass/fail thresholds

Per the skill's Section-7 threshold table for `module:` mode (the closest match to a single-test verification): **all filtered tests pass → PASS**.

RQ-002b is the only test in scope; it passes. **Verdict: PASS.**

---

## Baseline delta vs 2026-04-28-00-10-060-verify.md

| Surface | 2026-04-28 00:10Z (post-Feature-060) | 2026-04-28 07:45Z (post-Feature-061) | Verdict |
|---------|--------------------------------------|--------------------------------------|---------|
| `/workbench` Admin Dashboard tile label (en) | "Pending Review" (misleading) | "Pending Moderation" (honest) | ✅ MC-64 fix verified |
| Localization (uk/ru) | "Очікують перевірки" / "Ожидают проверки" (misleading) | "Очікують модерації" / "Ожидают модерации" (honest) | ✅ MC-64 fix verified across all 3 locales |
| Tile data source / click-through / subtitle | Unchanged | Unchanged | ✅ Out-of-scope changes confirmed not introduced |
| Other `pendingReview` keys in different namespaces | Preserved | Preserved | ✅ FR-006 honored |
| chat-backend `getTeamStats` patch (from Feature 060) | In place | Unchanged (in place) | ✅ FR-007 honored |

---

## Recommendations

- **MC-64**: ready to close in Jira with this report + the workbench-frontend PR #179 + the client-spec PR #199 as evidence.
- **MTB-1604** Epic + **MTB-1605**, **MTB-1606** Stories: ready to close.
- **MC-63** release Epic: comment that MC-64 is now closed; all 4 child bugs (MC-64..67) cleared; Review Queue UX is fully unblocked.
